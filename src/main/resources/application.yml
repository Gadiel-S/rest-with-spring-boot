cors:
  originPatterns: http://localhost:8080,http://localhost:3000,https://github.com/Gadiel-S
file:
  uploadDir: C:\temp\UploadDir
spring:
  application:
    name: rest-with-spring-boot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot?useTimezone=true&serverTimezone=UTC
    username: root
    password: 11082003
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB
logging:
  level:
    root: INFO
    # net.sf.jasperreports.engine.*: TRACE
    # root: WARN
    # br.com.Gadiel_S: DEBUG
spring-doc:
#  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true
